# Host port the API is published on (container always listens on 8000).
# On a box that already serves port 80 (e.g. the FaceAttendance stack), keep this at 8000
# and open 8000 in the security group, or put nginx in front for path-based routing.
HOST_PORT=8000
